# Autopsy scripts (not the live gate)

These files document the **wrong-object** 3% residual (chain + fitted Chua slopes + invented 0.85 cut). They are not the application gate.

Live gate: `python -m sim.run_sim` and `python verification/run_cross_proof.py`.
